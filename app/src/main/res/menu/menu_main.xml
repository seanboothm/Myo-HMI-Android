<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools" tools:context=".MainActivity">


    <item
        android:id="@+id/menu_settings_hand"
        android:icon="@drawable/mechanical_hand"
        android:orderInCategory="100"
        android:title="@string/action_settings"
        app:showAsAction="always">
        <menu>
            <item
                android:id="@+id/connect_hand"
                android:orderInCategory="100"
                android:title="Connect"
                app:showAsAction="never" />

            <item
                android:id="@+id/disconnect_hand"
                android:orderInCategory="100"
                android:title="Disconnect"
                app:showAsAction="never" />
        </menu>
    </item>
    <item
        android:id="@+id/menu_settings_myo"
        android:icon="@drawable/menu_myo_white"
        android:orderInCategory="100"
        android:title="@string/action_settings"
        app:showAsAction="always">
        <menu>
            <item
                android:id="@+id/connect_myo"
                android:orderInCategory="100"
                android:title="Connect"
                app:showAsAction="never" />

            <item
                android:id="@+id/disconnect_myo"
                android:orderInCategory="100"
                android:title="Disconnect"
                app:showAsAction="never" />
        </menu>
    </item>
</menu>
